# Data Files

Tracked data files are kept here instead of the repository root.

- `tests/`: files needed by regression tests.
- `images/`: image inputs used manually with IBAE.
- `mojave_fits/`: MOJAVE FITS input files bundled with the companion tools.

Generated outputs should not be placed here unless they are intentionally added as fixtures.
